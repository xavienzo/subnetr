% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_fc}
\alias{simulate_fc}
\title{Simulate a connectome study with planted predictor-associated subnetworks}
\usage{
simulate_fc(
  n,
  n_nodes = 100L,
  cluster_size = 20L,
  f2 = 0.05,
  rho_in = 0.9,
  rho_out = 0.02,
  f2_out = NULL,
  n_cov = 0L,
  method = c("fast", "data"),
  edge_corr = 0,
  shuffle_nodes = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Number of subjects.}

\item{n_nodes}{Number of nodes in the connectome.}

\item{cluster_size}{Integer vector of planted subnetwork sizes.}

\item{f2}{Cohen's \eqn{f^2} effect size for the affected edges, recycled
across subnetworks so different subnetworks can carry different effects.}

\item{rho_in}{Proportion of within-subnetwork edges that carry the effect.}

\item{rho_out}{Proportion of outside edges that carry the effect.}

\item{f2_out}{Effect size for the scattered outside edges. Defaults to
\code{f2[1]}.}

\item{n_cov}{Number of nuisance covariates adjusted for.}

\item{method}{\code{"fast"} or \code{"data"}; see the section on simulation method.}

\item{edge_corr}{Cross-edge noise correlation induced by a shared
subject-level factor. \code{method = "data"} only.}

\item{shuffle_nodes}{Randomly relabel nodes so the planted subnetworks are
scattered through the matrix.}

\item{seed}{Optional integer seed.}
}
\value{
A list of class \code{subnet_sim}:
\item{W}{\code{n_nodes} by \code{n_nodes} matrix of \eqn{-\log_{10}} p-values.}
\item{truth}{List with \code{nodes} (node indices of each planted subnetwork,
in the returned labelling), \code{edge_signal} (logical over vectorized
edges), and \code{labels} (block index per node, 0 for background).}
\item{data}{Subject-level \code{fc}, \code{x} and \code{covariates}, or \code{NULL} under
\code{method = "fast"}.}
\item{params}{The settings used, including the residual \code{df}.}
}
\description{
Generates a weighted adjacency matrix of \eqn{-\log_{10}} p-values in which
one or more subnetworks carry an association with a predictor of interest,
together with the ground truth needed to score a detection method.
}
\details{
Nodes 1 through \code{sum(cluster_size)} are assigned to consecutive planted
subnetworks. A proportion \code{rho_in} of the edges inside each subnetwork
actually carry the effect (the rest are within-subnetwork nulls, reflecting
that a real subnetwork is not uniformly affected), and a proportion
\code{rho_out} of the edges outside every subnetwork carry it too (scattered
associations that are not part of any subnetwork). Node labels are then
shuffled so the planted structure is not sitting in the corner of the matrix.
}
\section{Simulation method}{

\code{method = "fast"} (default) draws the edge t-statistics directly from their
exact sampling distribution rather than materializing subject-level data.
Under the linear model with fixed design, an edge with Cohen's
\eqn{f^2} effect size has
\deqn{t \sim t_{df}(\mathrm{ncp}), \quad \mathrm{ncp} = \sqrt{f^2 S},
  \quad S \sim \chi^2_{n - 1 - q},}
where \eqn{df = n - 2 - q} and \eqn{q} is the number of nuisance covariates.
\code{S} is drawn once per data set, which is what correctly couples all edges
through the shared predictor. This is exact, not an approximation, and it
avoids allocating an \code{n} by \code{n_edge} matrix -- the reason power analysis
over thousands of replicates is practical.

\code{method = "data"} builds the subject-level edge matrix explicitly and runs
\code{\link[=edge_stats]{edge_stats()}} on it. Use it when you need the raw data, or when
\code{edge_corr > 0} to study how the test behaves under cross-edge dependence,
which the fast path deliberately excludes.
}

\examples{
sim <- simulate_fc(n = 100, n_nodes = 80, cluster_size = c(20, 10),
                   f2 = c(0.15, 0.25), seed = 3)
dim(sim$W)
lengths(sim$truth$nodes)

}
\seealso{
\code{\link[=subnet]{subnet()}}, \code{\link[=power_curve]{power_curve()}}, \code{\link[=edge_stats]{edge_stats()}}
}
