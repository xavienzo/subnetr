% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.subnet}
\alias{plot.subnet}
\title{Plot the connectivity matrix}
\usage{
\method{plot}{subnet}(
  x,
  what = c("reordered", "observed", "both"),
  significant_only = TRUE,
  col = grDevices::hcl.colors(64, "Inferno"),
  main = NULL,
  weight_label = NULL,
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{subnet} object.}

\item{what}{\code{"reordered"} (default), \code{"observed"} for the original node
order, or \code{"both"} for the two side by side.}

\item{significant_only}{Outline only the significant subnetworks.}

\item{col}{Colour palette.}

\item{main}{Panel title, or a vector of two when \code{what = "both"}.}

\item{weight_label}{Label for the colour scale, as a string or an
\link{expression}. Defaults to the \code{"statistic"} attribute of \code{W}, and to
\code{"edge weight"} when the matrix carries none.}

\item{legend}{Draw the colour scale.}

\item{...}{Passed to \code{\link[graphics:image]{graphics::image()}}.}
}
\value{
\code{x}, invisibly. Called for the plot.
}
\description{
Draws the edge-weight matrix, optionally with nodes reordered so that the
extracted subnetworks sit in consecutive blocks along the diagonal, and
outlines the significant blocks.
}
\details{
\code{what = "both"} puts the matrix as measured next to the reordered version,
sharing one colour scale. That pairing is the honest way to present a
result: a subnetwork is invisible in the original node ordering and obvious
after reordering, and showing only the second panel can make an arbitrary
permutation look like a discovery. Comparing the two is also how you judge
whether a detection is a compact block or a diffuse smear.

The colour scale is labelled with the quantity being displayed. When \code{W}
came from \code{\link[=edge_stats]{edge_stats()}} or \code{\link[=simulate_fc]{simulate_fc()}} that label is carried along with
the matrix, so the figure states whether it is showing \eqn{-\log_{10}} p,
a t statistic, or something else. Supply \code{weight_label} for matrices built
by other means.
}
\examples{
sim <- simulate_fc(n = 120, n_nodes = 60, cluster_size = 12, f2 = 0.2,
                   seed = 11)
fit <- subnet(sim$W, n_perm = 99, seed = 1)

plot(fit)                  # reordered only
plot(fit, what = "both")   # as measured, next to reordered

# A matrix of t statistics from a real study labels itself accordingly
plot(fit, weight_label = "t statistic")

}
