% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.subnet_partition}
\alias{plot.subnet_partition}
\title{Plot the blocks of a partition}
\usage{
\method{plot}{subnet_partition}(
  x,
  what = c("reordered", "observed", "both"),
  blocks = seq_len(x$n_clusters),
  col = grDevices::hcl.colors(64, "Inferno"),
  main = NULL,
  weight_label = NULL,
  legend = TRUE,
  W = x$W,
  zlim = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{subnet_partition} object.}

\item{what}{\code{"reordered"} (default), \code{"observed"}, or \code{"both"}.}

\item{blocks}{Which blocks to outline. Defaults to every extracted
subnetwork, excluding the background block.}

\item{col}{Colour palette.}

\item{main}{Panel title, or a vector of two when \code{what = "both"}.}

\item{weight_label}{Label for the colour scale, as a string or an
\link{expression}. Defaults to the \code{"statistic"} attribute of the matrix, and
to \code{"edge weight"} when it carries none.}

\item{legend}{Draw the colour scale.}

\item{W}{Matrix to display. Defaults to the one the partition was built
from; supply another of the same dimensions to view it in this ordering,
which is how two groups are compared under a common partition.}

\item{zlim}{Range of the colour scale. Defaults to the range of \code{W}; fix it
to compare separate figures on a common scale.}

\item{...}{Passed to \code{\link[graphics:image]{graphics::image()}}.}
}
\value{
\code{x}, invisibly. Called for the plot.
}
\description{
Draws the weighted matrix a \code{\link[=subnet_extract]{subnet_extract()}} partition was built from,
with nodes reordered so the extracted blocks lie along the diagonal.
}
\details{
Unlike \code{\link[=plot.subnet]{plot.subnet()}}, no block is singled out as significant, because a
partition on its own carries no inference: every block is outlined. This is
the plot to use when extraction is applied to a connectivity matrix
directly, such as a group-average connectome, rather than to a matrix of
association statistics.
}
\examples{
sim <- simulate_fc(n = 100, n_nodes = 60, cluster_size = 12, f2 = 0.2,
                   seed = 1)
part <- subnet_extract(sim$W, threshold = quantile(vech(sim$W), 0.95))
plot(part)

}
