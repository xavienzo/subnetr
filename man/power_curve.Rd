% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{power_curve}
\alias{power_curve}
\title{Power analysis for subnetwork detection}
\usage{
power_curve(
  n,
  n_nodes = 100L,
  cluster_size = 20L,
  f2 = 0.05,
  rho_in = 0.9,
  rho_out = 0.02,
  f2_out = NULL,
  n_cov = 0L,
  n_sim = 200L,
  n_perm = 199L,
  alpha = 0.05,
  threshold_prob = NULL,
  lambda = 0.6,
  objective = c("generalized", "density"),
  min_size = 3L,
  max_clusters = 25L,
  tune_each = FALSE,
  tune = list(),
  dice_cut = 0.5,
  n_cores = 1L,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{n}{Integer vector of sample sizes to evaluate.}

\item{n_nodes, cluster_size, f2, rho_in, rho_out, f2_out, n_cov}{Passed to
\code{\link[=simulate_fc]{simulate_fc()}}, describing the connectome and the planted effect.}

\item{n_sim}{Replicates per sample size.}

\item{n_perm}{Permutations per replicate. \code{199} gives p-value resolution
\code{0.005}, ample for \code{alpha = 0.05}.}

\item{alpha}{Family-wise significance level.}

\item{threshold_prob}{Quantile of the observed edge weights used as the
screening threshold when \code{tune_each = FALSE}. \code{NULL} (the default) scales
it with the design so that screening retains roughly twice as many edges
as the planted subnetworks contain, capped at the top 10\%. See the section
on the screening threshold -- this is the single most consequential
setting in a power analysis.}

\item{lambda}{Objective parameter used when \code{tune_each = FALSE}.}

\item{objective, min_size, max_clusters}{Passed to \code{\link[=subnet]{subnet()}}.}

\item{tune_each}{Retune the threshold and \code{lambda} in every replicate with
\code{\link[=tune_subnet]{tune_subnet()}}, and pay the corresponding selection cost in the
permutation null. This simulates the procedure a real analysis runs, which
is the reason to use it; it is considerably slower, and the power it
reports can land either side of a fixed threshold.}

\item{tune}{A named list of extra arguments for \code{\link[=tune_subnet]{tune_subnet()}}, used only
when \code{tune_each = TRUE}. Shrinking its grid or its \code{n_perm} is the main
way to make \code{tune_each} affordable.}

\item{dice_cut}{Minimum Sorensen-Dice overlap for a planted subnetwork to
count as recovered.}

\item{n_cores}{Cores used to run replicates in parallel.}

\item{seed}{Optional integer seed. Results are reproducible and do not
depend on \code{n_cores}.}

\item{progress}{Print a line per sample size.}
}
\value{
An object of class \code{subnet_power}:
\item{power}{Data frame with one row per sample size: \code{n}, \code{power},
\code{power_recovery}, their Monte Carlo standard errors \code{se_power} and
\code{se_recovery}, \code{mean_dice}, \code{mean_n_sig} and \code{false_subnet_rate}, the
average proportion of significant subnetworks that match no planted
one.}
\item{by_cluster}{Data frame with one row per sample size and planted
subnetwork, giving that subnetwork's own recovery probability and mean
overlap.}
\item{replicates}{Per-replicate results, for diagnostics.}
\item{params}{The settings used.}
}
\description{
Estimates, by simulation, the probability that a study of a given size
detects a predictor-associated subnetwork of a given size and effect size.
}
\details{
For each sample size and each replicate the function simulates a connectome
study with \code{\link[=simulate_fc]{simulate_fc()}}, runs the full \code{\link[=subnet]{subnet()}} pipeline including its
permutation test, and records what was found. Two quantities are reported
because they answer different questions:

\describe{
\item{\code{power}}{the probability of declaring \emph{any} subnetwork significant.
This is the conventional notion of power, but on its own it can be
satisfied by a subnetwork that has little to do with the planted one.}
\item{\code{power_recovery}}{the probability that every planted subnetwork is
matched by a significant one with Sorensen-Dice overlap at least
\code{dice_cut}. This is the quantity you want when the scientific claim is
about \emph{which} nodes are involved, and it is always the more demanding of
the two.}
}

The gap between them is informative: when \code{power} is high but
\code{power_recovery} is not, the study is large enough to see that something is
there but not to localize it.

Replicates are independent, so the Monte Carlo standard error of each
proportion is reported and shrinks as \code{1 / sqrt(n_sim)}. Roughly 200
replicates give a standard error near 0.035 at 50\% power.
}
\section{The screening threshold}{

Nothing else in the analysis moves power as much as \code{threshold_prob}. A
subnetwork of \code{c} nodes spans \code{c(c-1)/2} edges; if screening retains fewer
edges than that across the whole graph, the subnetwork cannot survive as a
block no matter how large the sample. In an 80-node connectome with a
20-node planted subnetwork at f2 = 0.06 and n = 120, recovery probability
runs

\tabular{lr}{
\code{threshold_prob} \tab recovery \cr
0.99  \tab 0.30 \cr
0.975 \tab 0.95 \cr
0.95  \tab 1.00 \cr
}

purely from where the threshold sits: the subnetwork spans 190 edges, and at
the 99th percentile only 32 edges survive screening in the entire graph. The
\code{NULL} default scales the threshold with the design to keep this from
silently dominating the answer, but a reported power is a statement about a
\emph{particular} screening choice and should be quoted alongside it.

If you intend to tune the threshold at analysis time rather than fix it in
advance, set \code{tune_each = TRUE} so the simulation runs the procedure you
will actually use. That is slower, and it can come out either higher or
lower than a fixed threshold -- permutations now pay the same search cost,
which costs power, but the threshold adapts to each data set, which gains
it. The reason to use it is fidelity, not conservatism.
}

\examples{
\donttest{
pw <- power_curve(n = c(50, 100), n_nodes = 60, cluster_size = 12,
                  f2 = 0.2, n_sim = 20, n_perm = 99, seed = 1)
pw
}

}
\seealso{
\code{\link[=required_n]{required_n()}}, \code{\link[=plot.subnet_power]{plot.subnet_power()}}, \code{\link[=simulate_fc]{simulate_fc()}}, \code{\link[=subnet]{subnet()}}
}
