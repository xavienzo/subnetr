% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_stats.R
\name{edge_stats}
\alias{edge_stats}
\title{Edge-wise association statistics for a connectivity study}
\usage{
edge_stats(fc, x, covariates = NULL, value = c("nlog10p", "t", "p"))
}
\arguments{
\item{fc}{Connectivity data. Either an \code{n_subject} by \code{n_edge} matrix whose
rows are vectorized lower triangles (see \code{\link[=vech]{vech()}}), or an
\code{n_node} by \code{n_node} by \code{n_subject} array of connectivity matrices.}

\item{x}{Numeric predictor of interest, length \code{n_subject}.}

\item{covariates}{Optional numeric matrix or data frame of nuisance
covariates with \code{n_subject} rows. An intercept is always included and must
not be supplied.}

\item{value}{What to return in the matrix: \code{"nlog10p"} (default),
\code{"t"}, or \code{"p"}.}
}
\value{
A symmetric \code{n_node} by \code{n_node} matrix with a zero diagonal,
carrying attributes \code{df} (residual degrees of freedom), \code{t} (the vector of
edge t-statistics) and \code{beta} (the vector of edge slopes).
}
\description{
Regresses every edge of a connectome on a predictor of interest while
adjusting for nuisance covariates, and returns the resulting evidence as a
weighted adjacency matrix suitable for \code{\link[=subnet]{subnet()}}.
}
\details{
The model fitted at each edge \eqn{e} is
\deqn{y_e = \beta_{0e} + \beta_e x + Z \gamma_e + \varepsilon_e.}
Rather than looping over edges, the predictor and the connectivity matrix
are both residualized on \code{cbind(1, covariates)} once, after which the
Frisch-Waugh-Lovell theorem gives every edge's slope, standard error and
t-statistic in a handful of vectorized operations. The cost is one QR
factorization of an \code{n * (p + 1)} matrix plus two BLAS-level products,
independent of the number of edges.

P-values are computed on the log scale, so evidence far beyond double
precision -- routine in connectome studies with thousands of edges -- is
represented exactly instead of saturating at \code{Inf}.
}
\examples{
set.seed(1)
n <- 60; p <- 20; m <- p * (p - 1) / 2
x <- rnorm(n)
fc <- matrix(rnorm(n * m), n, m)
fc[, 1:10] <- fc[, 1:10] + 0.6 * x        # plant a signal
W <- edge_stats(fc, x, covariates = cbind(age = rnorm(n)))
dim(W)
round(max(W), 2)

}
\seealso{
\code{\link[=subnet]{subnet()}}, \code{\link[=simulate_fc]{simulate_fc()}}
}
