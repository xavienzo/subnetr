% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{vech}
\alias{vech}
\alias{unvech}
\title{Convert between a symmetric matrix and its vectorized lower triangle}
\usage{
vech(W)

unvech(v)
}
\arguments{
\item{W}{A symmetric numeric matrix with a zero diagonal.}

\item{v}{A numeric vector of length \code{n * (n - 1) / 2}.}
}
\value{
\code{vech()} returns a numeric vector of length \code{n * (n - 1) / 2};
\code{unvech()} returns a symmetric numeric matrix with a zero diagonal.
}
\description{
\code{vech()} extracts the strict lower triangle of a symmetric matrix in
column-major order and \code{unvech()} rebuilds the matrix. This is the ordering
the package uses for every edge vector, and it matches \code{W[lower.tri(W)]} in
base R and \code{squareform()} in MATLAB, so edge vectors can be exchanged with
either without reindexing.
}
\examples{
W <- matrix(0, 4, 4)
W[lower.tri(W)] <- 1:6
W <- W + t(W)
vech(W)
identical(unvech(vech(W)), W)

}
