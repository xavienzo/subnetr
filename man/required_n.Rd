% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{required_n}
\alias{required_n}
\title{Sample size required to reach a target power}
\usage{
required_n(object, target = 0.8, which = c("recovery", "any"))
}
\arguments{
\item{object}{A \code{subnet_power} object.}

\item{target}{Target power, e.g. \code{0.8}.}

\item{which}{Which power curve to read: \code{"recovery"} (default, the
probability of recovering the planted subnetworks) or \code{"any"} (the
probability of any detection).}
}
\value{
A single number: the interpolated sample size, \code{NA} with a warning
if the curve never reaches \code{target} over the range simulated.
}
\description{
Interpolates a \code{\link[=power_curve]{power_curve()}} to report the smallest sample size reaching a
target power.
}
\details{
Linear interpolation between the two bracketing sample sizes. Extend the
grid rather than trusting an extrapolation if the target lies outside it.
}
\examples{
\donttest{
pw <- power_curve(n = c(40, 80, 120), n_nodes = 60, cluster_size = 12,
                  f2 = 0.2, n_sim = 20, n_perm = 99, seed = 1)
required_n(pw, target = 0.8)
}

}
